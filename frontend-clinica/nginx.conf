server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Esta es la magia: si no encuentra el archivo o carpeta, redirige al index.html
        try_files $uri $uri/ /index.html;
    }
}